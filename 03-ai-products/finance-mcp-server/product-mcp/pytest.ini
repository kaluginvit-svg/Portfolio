[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    integration: integration-style tests against registry and sqlite
    scenario: heavy scenario (also run via run_scenarios.py)
