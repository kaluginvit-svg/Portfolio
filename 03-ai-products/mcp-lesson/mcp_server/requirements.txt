# MCP SDK: инструменты и JSON Schema для хоста (FastMCP)
mcp[cli]>=1.2.0
uvicorn[standard]>=0.32.0

# По заданию урока
openai>=1.0.0
fastapi>=0.115.0
python-multipart>=0.0.9

# sqlite3 входит в стандартную библиотеку Python — отдельно не устанавливается.
# Пакета mcp-spec в PyPI нет; протокол реализует пакет mcp (modelcontextprotocol).
