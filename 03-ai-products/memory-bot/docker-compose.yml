name: nemo-bot

services:
  nemo-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      SQLITE_PATH: /app/data/bot.db
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
