# Секреты и конфиг (в контейнер не попадают; токен передаётся через env_file)
.env
.env.*
!.env.example

# Виртуальное окружение
venv
.venv
env

# Логи (создаются в рантайме)
logs

# Git
.git
.gitignore

# Устаревшая папка bot (код перенесён в корневой main.py)
bot

# IDE и кэш
.cursor
.vscode
.idea
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
.eggs
dist
build

# Документация и прочее (не нужны в образе)
*.md
!README.md
